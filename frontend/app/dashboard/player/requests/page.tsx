import { PlaceholderDashboardPage } from "@/components/PlaceholderDashboardPage";

export default function PlayerRequestsPage() {
  return (
    <PlaceholderDashboardPage
      title="My Games"
      description="Open-game join requests will live in this section as the games workflow is completed."
    />
  );
}
