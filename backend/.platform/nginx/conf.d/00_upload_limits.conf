# The profile editor accepts images up to 2 MiB. Leave headroom for the
# multipart boundary so Nginx does not reject valid uploads before Django can
# apply its server-side 2 MiB validation.
client_max_body_size 3m;
