# Individual images are limited to 5 MiB in Django. Venue galleries can submit
# several valid photos together, so the gateway leaves practical batch headroom
# and lets the application enforce each file's real limit.
client_max_body_size 32m;
